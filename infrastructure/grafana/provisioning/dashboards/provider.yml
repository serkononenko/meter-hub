# Loads every dashboard JSON under this directory on startup.
apiVersion: 1

providers:
  - name: meterhub
    folder: MeterHub
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
