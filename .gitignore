.idea
.env
.DS_Store

# Local development key material — never commit (dev-generated key pair; real
# environments deliver keys from a secret manager, not this repo)
/certs/

# Playwright MCP session artifacts (snapshots, console logs, screenshots)
.playwright-mcp/
