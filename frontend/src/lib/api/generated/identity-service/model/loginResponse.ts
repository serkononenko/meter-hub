/**
 * Generated by orval 🍺
 * Do not edit manually.
 * MeterHub Identity Service API
 * Identity Service HTTP API. Covers user registration, authentication, and token management.
 * OpenAPI spec version: 1.0.0
 */
import type { LoginResponseTokenType } from './loginResponseTokenType';
import type { User } from './user';

export interface LoginResponse {
  /** Signed JWT access token. Finite lifetime; keep it client-side only. */
  accessToken: string;
  /** Opaque refresh token. Single use — presenting it to /auth/refresh returns a new pair and invalidates this token. Store it securely. */
  refreshToken: string;
  /** How the client sends the token. Always Bearer. */
  tokenType: LoginResponseTokenType;
  /** Access token lifetime in seconds from issuance. */
  expiresIn: number;
  user: User;
}
